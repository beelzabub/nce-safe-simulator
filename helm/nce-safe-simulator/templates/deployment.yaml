apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      # The app makes AWS API calls via IRSA, which injects its own projected
      # token — the default kube-api token is unused, so don't mount it.
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: {{ .Release.Name }}
          image: "{{ .Values.image.repository }}:{{ required "image.tag is required — pass the immutable tag ecr-push printed (issue #290)" .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["sh", "/app/scripts/entrypoint-eks.sh"]
          # The container still runs as root to bind privileged port 80 (see
          # #287 triage: non-root would require an app-wide port move). Harden
          # what is compatible with that: no privilege escalation, and drop all
          # Linux capabilities except the one needed to bind port 80.
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
              add:
                - NET_BIND_SERVICE
          ports:
            - containerPort: 80
              protocol: TCP
          env:
            - name: CONFIG_PARAM
              value: {{ .Values.config.param | quote }}
            - name: AWS_DEFAULT_REGION
              value: {{ .Values.config.awsRegion | quote }}
            {{- if .Values.config.grafanaUrl }}
            - name: GRAFANA_URL
              value: {{ .Values.config.grafanaUrl | quote }}
            {{- end }}
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu | quote }}
              memory: {{ .Values.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.resources.limits.cpu | quote }}
              memory: {{ .Values.resources.limits.memory | quote }}
          volumeMounts:
            - name: nce-config
              mountPath: /mnt/config
            - name: nce-reports
              mountPath: /app/reports
            - name: nce-interactive
              mountPath: /app/public/interactive
            - name: nce-quarto-site
              mountPath: /app/quarto-site
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 30
      volumes:
        - name: nce-config
          persistentVolumeClaim:
            claimName: nce-config
        - name: nce-reports
          persistentVolumeClaim:
            claimName: nce-reports
        - name: nce-interactive
          persistentVolumeClaim:
            claimName: nce-interactive
        - name: nce-quarto-site
          persistentVolumeClaim:
            claimName: nce-quarto-site
