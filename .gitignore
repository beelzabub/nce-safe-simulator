__pycache__/
*.pyc
*.pyo
.env

# Real config holds the GitLab token — keep it out of git. Commit config.example.json instead.
/config.json
# Personal shell config — nothing here is needed to build or run the project, and
# it is exactly the kind of file that carries exported credentials.
/.zshrc
# Local Python virtualenv (README installation step 2).
/.venv/
cdk/cdk.out/
cdk/cdk-ecs.out/
cdk/cdk-eks.out/
cdk/.venv/

/.quarto/
**/*.quarto_ipynb*
/public/
/quarto-site/
/quarto-data/
/logs/
/reports/
/uploads/

**/*.quarto_ipynb

# deck/ build outputs - regenerated by capture_screenshots.py / fetch_metrics.py / build_deck.py,
# not source. See deck/README.md.
deck/screenshots/
deck/metrics.json
deck/dist/
deck/.template-cache/
version.json
